package tests;

import dto.WorkoutCalculators;
import dto.WorkoutCalculatorsFactory;
import org.testng.annotations.Test;


public class WorkoutCalculatorsTest extends BaseTest {

    /*@Test(description = "Checking that the Intensity Calculator is functioning correctly when fields are filled in with valid data")
    public void intensityCalculatorShouldBeFilledInSuccessfully () {
        loginSteps.login(USER, PASSWORD);
        WorkoutCalculators workoutCalculators = WorkoutCalculatorsFactory.getCalculatorsData();
        workoutCalculatorsSteps.calculateWorkoutData(workoutCalculators);
    }*/
}
